language: node_js
node_js:
  - "6.2"

sudo: required
services:
  - docker

install:
  - docker build -t gtrias/docker-api-registar .
  - docker logs docker-api-registar
  - npm install

script:
  - docker run -d --name docker-api-registar gtrias/docker-api-registar
  - docker ps | grep -q  docker-api-registar

after_success:
  - if [ "$TRAVIS_BRANCH" == "master" ]; then
    docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD";
    docker push gtrias/docker-api-registar;
    fi
